'use strict';

describe('myApp.listado module', function() {

  beforeEach(module('myApp.crear'));

  describe('listado controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var listadoCtrl = $controller('CrearCtrl');
      expect(listadoCtrl).toBeDefined();
    }));

  });
});